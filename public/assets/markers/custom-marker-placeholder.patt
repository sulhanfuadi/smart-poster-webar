# Replace this placeholder with a real generated AR.js pattern file.
# Current config already points to this file in pattern mode.
# Recommended workflow:
# 1. Finalize poster marker image.
# 2. Generate .patt from that marker image.
# 3. Overwrite this file while keeping the same path.
